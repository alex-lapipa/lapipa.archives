{
  "name": "lapipa-archives",
  "version": "0.1.0",
  "private": true,
  "engines": { "node": ">=24" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check api/health.mjs && node --check api/search.mjs && node --check scripts/build.mjs",
    "vercel-build": "npm run build"
  }
}

